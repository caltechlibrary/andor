#
# This is an example andor.toml file for testing.
#

port = "8246"
host = "localhost"
protocol = "http"
users_toml = "testdata/users.toml"
workflows_toml = "testdata/workflows.toml"
repository = [ "repository.ds" ]

